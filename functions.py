import numpy as np


def gauss(x, mu, sigma):
    """
    Fill this in
    """
    pass


def fit_gauss(xdata, ydata):
    """
    Fit Gaussian to data and return the fit parameters
    and uncertainties in them.

    Parameters
    ----------

    xdata : numpy array
        Positions at which the ydata is sampled.

    ydata : numpy array
        Values to be fit to Gaussian.

    Returns
    -------

    fit_parameters, uncertainty : two lists
        Best fit parameters and uncertainty in them.
    """
    pass
